# Building AI Agents for Network Operations - Python Dependencies
# Install with: pip install -r requirements.txt

# Labs 1-4 use the TJU competition API configured in the project .env file.

# General HTTP clients used by MCP and external examples
requests>=2.32.0

# TJU Competition API (OpenAI-compatible Chat Completions)
openai>=1.0,<3

# TJU NetPilot application API and validated environment settings
fastapi>=0.115,<1
pydantic>=2.9,<3
pydantic-settings>=2.6,<3
httpx>=0.27,<1
psutil>=5.9,<8
dnspython>=2.6,<3

# NetPilot Milestone 5 local Chinese embeddings and vector search
fastembed>=0.7,<1
faiss-cpu>=1.9,<2
numpy>=1.26,<3

# Upstream external API examples
anthropic>=0.104.0
python-dotenv>=1.0.1

# Network automation and config parsing examples
netmiko>=4.4.0
PyYAML>=6.0.2
rich>=13.9.0

# MCP server, HTTP bridge, and browser UI examples
mcp[cli]>=1.9,<2
starlette>=0.37.0
uvicorn>=0.30.0

# Test suite
pytest>=8.3.0
